# 위탁 대시보드 — 노트북 워커 명세 (직원14용, 2026-07-15 T봇 작성)

> 대시보드(폰·데스크톱)에서 [주문 수집]을 누르면 **작업 티켓**만 만들어진다.
> 실제 테무 수집은 **노트북**이 한다(테무 로그인 크롬이 거기에만 있음).
> 이 문서는 그 워커가 지켜야 할 계약(contract)이다. **아직 미구현 — 사장님 방식 확정 후 착수.**

## 0. 대전제 (미결)
직원14 보고: **테무 브라우저 자동조작 = SHARED_RULES 1조 위반**(계정정지·정산동결 리스크).
따라서 아래 3안 중 사장님이 고른 방식으로만 구현한다. **임의로 자동조작 강행 금지.**

| 안 | 방식 | 리스크 |
|---|---|---|
| **A (권장)** | 사장님이 셀러센터에서 주문 CSV 다운로드 → 대시보드에 드래그앤드롭 → 양식 자동작성 → 다운로드 | 없음. 사람 클릭 1회 |
| B | 테무 셀러 **오픈API** 사용 (존재 여부 미조사) | 없음(있다면). 조사 필요 |
| C | 브라우저 자동조작 | 계정정지·정산동결 |

※ A안이면 워커조차 필요 없다(브라우저에서 전부 처리 가능). 아래는 B/C안일 때의 명세.

## 1. RTDB 계약
```
shared/wholesale/jobs/{jobId} = {
  vendorId:   "dm",                     // 거래처 id
  vendorName: "(주)대명유통 · 홍지민",
  status:     "queued" | "running" | "done" | "error",
  requestedAt:"2026-07-15T12:00:00.000Z",
  hasForm:    true,                     // 이 거래처 주문양식 업로드 여부
  // ↓ 워커가 채운다
  count:      37,                       // 수집 건수 (실측. 0이면 0)
  file:       "data:application/vnd...", // 작성된 주문서 (base64 data URL)
  fileName:   "대명유통_주문서_20260715.xlsx",
  error:      "사유"                     // status=error 일 때 필수
}
shared/wholesale/forms/{vendorId} = { name, size, type, data(base64), at }
shared/wholesale/vendors  = [ {id, name, shipDeadline, ...} ]   ← 단일 소스
shared/wholesale/products = [ {pid, vendorId, buy, sell, ...} ] ← 단일 소스
```

## 2. 워커 루프
1. `shared/wholesale/jobs` 를 5~10초 간격 폴링 (`status == "queued"` 만)
2. 잡으면 즉시 `status = "running"` (중복 처리 방지)
3. 테무에서 해당 거래처 관련 주문 수집 (**확정된 방식으로만**)
4. `forms/{vendorId}` 양식을 읽어 **그 거래처 컬럼 구조에 맞춰** 작성
   - 양식 없으면(`hasForm:false`) 공통 기본 형식으로
5. 결과 파일을 base64로 `file`에 넣고 `status="done"`, `count=실제건수`
6. 실패 시 `status="error"`, `error="구체적 사유"` — **조용한 실패 절대 금지**

## 3. 철칙
- **지어내기 금지**: 수집 0건이면 `count:0, status:"done"`. 가짜 행 채우지 말 것.
- **조용한 실패 금지**: 로그인 만료·차단이면 `status:"error"` + 사유. (07-13 직원10 사고와 같은 패턴 반복 금지)
- **파일 크기**: RTDB 1노드 권장 10MB 이하. 초과 시 CSV로 축소하거나 분할.
- **완료된 job 정리**: `done` 후 7일 지난 티켓은 워커가 삭제(무한 누적 방지).
- **노트북 꺼짐**: 대시보드가 `queued`로 계속 표시 → 켜지면 자동 처리(정상 동작).

## 4. 대시보드 쪽은 이미 완료됨 (T봇, 2026-07-15)
- [주문 수집] 버튼 → 티켓 생성 ✅ (라이브 검증됨)
- 첫 화면 작업 표시줄: ⏳대기중 / ⚙️수집중 / ✅완료+⬇다운로드 / ❌실패+사유 ✅
- 양식 드래그앤드롭 업로드 ✅
- 워커가 위 계약대로 채우기만 하면 폰에서 바로 다운로드된다.
